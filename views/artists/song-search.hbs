{{#each result}}
	<figure>
		<figcaption>Listen to {{this.name}} by {{this.artists.0.name}}</figcaption>
		<audio controls src="{{this.preview_url}}"><a href="{{this.href}}">Play Audio</a></audio>
	</figure>
            <img src="{{this.album.images.0.url}}" alt="">
{{/each}}